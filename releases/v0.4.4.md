## Improvements

- **Logbook Container Image** - The logbook service is now published to `ghcr.io/windshiftapp/logbook` with the same version tags as the main image, built in parallel across both CI/CD and the release script
